export enum ChatType {
    Direct = "Direct",
    Group = "Group",
    Channel = "Channel"
}

export enum ChatRole {
    Admin = "Admin",
    Moderator = "Moderator",
    User = "User"
}

export enum ContactStatus {
    Pending = "Pending",
    Active = "Active",
    Blocked = "Blocked",
    Archived = "Archived"
}


