{
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "dev:check": "echo 'Environment Check:' && echo 'USER_SERVICE_URL:' $USER_SERVICE_URL && echo 'MESSAGE_SERVICE_URL:' $MESSAGE_SERVICE_URL && echo 'SOCKET_URL:' $NEXT_PUBLIC_SOCKET_URL && npm run dev",
    "env:local": "cp .env.development .env.local && echo 'Switched to local development environment'",
    "env:docker": "cp .env .env.local && echo 'Switched to docker environment'"
  }
}
